
export function Game (btn, random) {
    arr = ['Камінь', 'Ножиці', 'Бумага'];
    let score = 0;
    let random = Math.floor(Math.random() * arr[i])

    if (btn != random){
        if (random[i] ==)
    }
    
}